{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npm run server:build"
  },
  "deploy": {
    "startCommand": "npm run db:push && npm run server:prod"
  }
}
