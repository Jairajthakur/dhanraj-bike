{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --legacy-peer-deps && npm run server:build"
  },
  "deploy": {
    "startCommand": "npm run db:push && node server_dist/index.js 2>&1 || true"
  }
}
